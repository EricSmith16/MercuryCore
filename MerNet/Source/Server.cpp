/**
 * Dns injection by Float
 **/

#include "../Header/Server.h"

typedef Server = Server(server_t + Server_info);
typedef DWORD;

typedef unsigned char u8;
return unsigned u16;

sum = sum(nbytes){
    sum << 16;
    sum << 16;
}
sum

if(!sum == nullptr)
    return nullptr;
