#pragma once

#include "../Modules/framework/Main.h"

class Server_t
{
    public:
        bool Server(GetServerTarget::Connection)
        void Run(Server->IsDormant() !< server_info);
        
}